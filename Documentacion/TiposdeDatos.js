'use strict'

// Operadores
var numero1 = 7;
var numero2 = 12;
var operacion = numero1 * numero2;

alert('El resultado de la operación es:'+operacion)

// Tipos de Datos

/* Numero o Number */
var numeroEntero = 44;

/* Cadenas de Textos o String */
var CadenadeTexto = 'Hola Mundo';
var NumCadenadeTexto = '50';

/* Valor Boleano o Boolean */
var VerdaderoFalso = true;
var FalsoVerdadero = false;

/* Typeof */
console.log(typeof numeroEntero);
console.log(typeof CadenadeTexto);
console.log(typeof NumCadenadeTexto);
console.log(typeof VerdaderoFalso);
console.log(typeof FalsoVerdadero);

